import { LegalPage } from "@/components/public/legal-page";
import { getPublicOperationsInfo } from "@/lib/config/env";

export default function PrivacyPage() {
  const operations = getPublicOperationsInfo();
  return <LegalPage title="Privacy Policy" intro="This policy explains how VoxMint handles account data, source samples and generated audio."><section><h2>Data we process</h2><ul><li>Account profile and authentication identifiers, including whether the identity provider verified an email.</li><li>Voice names, language, consent assertion and provider voice identifier.</li><li>Scripts, generation metadata, usage ledger entries and generated audio.</li></ul></section><section><h2>Voice samples</h2><p>Source audio is validated on the server and sent to the configured voice provider. VoxMint does not retain the source sample after the voice-creation request completes by default.</p></section><section><h2>Providers and storage</h2><p>When configured for production, selected samples and scripts are sent to Cartesia to create voices and speech. Generated audio is stored in a private R2-compatible bucket and streamed through an authenticated owner-checked route.</p></section><section><h2>Deletion and retention</h2><p>You can delete voices, individual generations, and your account. An automated retention preference is available only when this deployment has an active scheduled worker.</p></section><section><h2>Privacy contact</h2>{operations.privacyContactEmail ? <p>Privacy questions may be sent to <a className="text-[#af82f2]" href={`mailto:${operations.privacyContactEmail}`}>{operations.privacyContactEmail}</a>.</p> : <p className="text-[var(--warning)]">A monitored privacy contact is not configured for this private/development deployment.</p>}</section></LegalPage>;
}
