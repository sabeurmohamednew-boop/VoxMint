"use client";

import { LogOut, Save, ShieldAlert } from "lucide-react";
import { signOut } from "next-auth/react";
import { useState } from "react";
import { ConfirmDialog } from "@/components/ui/confirm-dialog";
import { useToast } from "@/components/ui/toast";
import { fetchJson } from "@/lib/api/client";

type Account = { name: string | null; email: string | null; preferredLanguage: string; preferredAudioFormat: string; theme: "SYSTEM" | "DARK" | "LIGHT"; retentionDays: number | null };

export function SettingsClient({ account }: { account: Account }) {
  const [form, setForm] = useState({ name: account.name ?? "", preferredLanguage: account.preferredLanguage, preferredAudioFormat: account.preferredAudioFormat, theme: account.theme, retentionDays: account.retentionDays ? String(account.retentionDays) : "none" });
  const [busy, setBusy] = useState(false); const [deleteOpen, setDeleteOpen] = useState(false); const { showToast } = useToast();
  async function save(event: React.FormEvent) { event.preventDefault(); setBusy(true); try { await fetchJson("/api/account", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...form, retentionDays: form.retentionDays === "none" ? null : Number(form.retentionDays) }) }); document.documentElement.classList.toggle("light", form.theme === "LIGHT"); showToast("Settings saved"); } catch (error) { showToast(error instanceof Error ? error.message : "Settings could not be saved.", "error"); } finally { setBusy(false); } }
  async function deleteAccount() { try { await fetchJson<void>("/api/account", { method: "DELETE" }); showToast("Account deleted"); await signOut({ redirectTo: "/" }); } catch (error) { showToast(error instanceof Error ? error.message : "Account deletion failed.", "error"); } }
  return <><form onSubmit={save} className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><section className="panel p-5 sm:p-6"><h2 className="text-base font-semibold">Profile &amp; preferences</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-[13px] font-semibold">Display name<input className="field mt-2 px-3" value={form.name} minLength={2} maxLength={80} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><label className="text-[13px] font-semibold">Email<input className="field mt-2 px-3 opacity-70" value={account.email ?? ""} disabled /></label><label className="text-[13px] font-semibold">Default language<select className="field mt-2 px-3" value={form.preferredLanguage} onChange={(event) => setForm({ ...form, preferredLanguage: event.target.value })}><option value="en">English</option><option value="fr">French</option><option value="ar">Arabic</option></select></label><label className="text-[13px] font-semibold">Preferred output<select className="field mt-2 px-3" value={form.preferredAudioFormat} onChange={(event) => setForm({ ...form, preferredAudioFormat: event.target.value })}><option value="wav">WAV</option><option value="mp3">MP3</option></select></label><label className="text-[13px] font-semibold">Theme<select className="field mt-2 px-3" value={form.theme} onChange={(event) => setForm({ ...form, theme: event.target.value as Account["theme"] })}><option value="DARK">Dark</option><option value="LIGHT">Light</option><option value="SYSTEM">System</option></select></label><label className="text-[13px] font-semibold">Retention preference<select className="field mt-2 px-3" value={form.retentionDays} onChange={(event) => setForm({ ...form, retentionDays: event.target.value })}><option value="none">Keep until I delete</option><option value="7">7 days</option><option value="30">30 days</option><option value="90">90 days</option></select></label></div><p className="mt-3 text-[11.5px] leading-5 text-[var(--muted)]">Retention preferences are saved, but scheduled deletion infrastructure is not active yet. You can delete audio manually from History.</p><button className="button-primary mt-5 px-5" disabled={busy}><Save className="h-4 w-4" />{busy ? "Saving…" : "Save settings"}</button></section><aside className="space-y-5"><section className="panel p-5"><h2 className="text-base font-semibold">Session</h2><p className="mt-2 text-sm leading-6 text-[var(--foreground-secondary)]">Sign out of this browser. Your saved voices and history will remain.</p><button type="button" className="button-secondary mt-4 px-4" onClick={() => void signOut({ redirectTo: "/" })}><LogOut className="h-4 w-4" />Sign out</button></section><section className="rounded-[14px] border border-[var(--danger)]/35 bg-[var(--danger)]/5 p-5"><div className="flex items-center gap-2 text-[var(--danger)]"><ShieldAlert className="h-5 w-5" /><h2 className="font-semibold">Delete account</h2></div><p className="mt-2 text-sm leading-6 text-[var(--foreground-secondary)]">Permanently delete provider voices, stored audio, and access to this account.</p><button type="button" className="button-secondary mt-4 border-[var(--danger)]/50 px-4 text-[var(--danger)]" onClick={() => setDeleteOpen(true)}>Delete account</button></section></aside></form><ConfirmDialog open={deleteOpen} onOpenChange={setDeleteOpen} title="Delete your VoxMint account?" description="This removes your saved voices and generated audio. This action cannot be undone." confirmLabel="Delete my account" danger onConfirm={deleteAccount} /></>;
}
